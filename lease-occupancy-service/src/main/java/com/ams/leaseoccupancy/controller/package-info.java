/**
 * REST controllers exposing the public and internal lease/occupancy endpoints.
 * Controllers only handle request/response mapping and delegate all business logic to the service layer.
 */
package com.ams.leaseoccupancy.controller;
